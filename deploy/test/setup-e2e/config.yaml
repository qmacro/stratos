endpoint:
  url: https://api.local.pcfdev.io
  skip-ssl: true
  admin-user:
    username: admin
    password: admin
  uaa:
    url: https://uaa.local.pcfdev.io
    zoneId: uaa
    client: admin
    client-secret: admin-client-secret
    cf-client: cf
    cf-client-secret: 
    skip-ssl: true
fixture:
  non-admin-user:
    username: e2e
    password: changeme
  organization: e2e
  space: e2e
  # Org and space, where the private and space-scoped service instances are not avialable
  no-access-org: e2e-test
  no-access-space: e2e-test
  services:
    public-service: public-service
    private-service: private-service
    space-scoped-service: space-scoped-service
